<types namespace="_exif">
  <type name="HandleBinaryFile" desc="Options for the `handleBinaryFile` method.">
    <prop boolean name="parseDates" default="false">
      Parse EXIF dates into JS dates.
    </prop>
    <prop string name="coordinates" default="dms">
      Return coordinates either as DMS (degrees, minutes, seconds) or DD (decimal degrees). Specified as `'dms'` or `'dd'`.
    </prop>
  </type>
</types>