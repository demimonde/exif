<types>
  <type name="Config" desc="Options for the program.">
    <prop boolean name="parseDates" default="false">
      Parse EXIF dates into JS dates.
    </prop>
    <prop type="('dms'|'dd')" name="coordinates" default="dms">
      Return coordinates either as DMS (degrees, minutes, seconds) or DD (decimal degrees).
    </prop>
  </type>
</types>